# Session Handoff — July 29, 2026 (Final)

## P1: Online Status Fix (OpenCodeWEBsUI)

**Status:** ✅ Complete, deployed to `pocwu.pages.dev`

**Commits (on `main`):**
- `0085e38` — fix(users): online status via WS presence KV + polyglot JS utility + live polling
- `1c2cb40` — fix(security): stop storing GitHub access token in session
- `89a3cb0` — fix(auth): reduce OAuth scope to read:user only, drop read:org

---

## P2: OpenCodeWEBsAG — Autonomous GitHub Bot

**Status:** ✅ All tasks complete

### Components Built

| Component | Location | Status |
|-----------|----------|--------|
| Core library | `src/` | ✅ Built, type-checks, 0 vulns |
| Webhook Worker | `worker/` | ✅ **Deployed** → `opencodewebsag-worker.xup.workers.dev` |
| AG UI Routes | `functions/api/ag/` | ✅ Built, wired into wrangler.toml |
| AG Dashboard | `src/pages/AGDashboard.tsx` | ✅ Route /ag → React dashboard |
| Rust WASM Crypto | `crypto/` | ✅ Built, 3/3 Rust tests pass |
| Docs | `OpenCodeWEBsPRD/*.md` | ✅ Both rewritten with current architecture |

### Architecture Decisions

| Aspect | Choice |
|--------|--------|
| App type | GitHub App (JWT + install tokens + webhooks) |
| Deployment | UI at `pocwu.pages.dev/ag`, worker at `opencodewebsag-worker.xup.workers.dev` |
| Polyglot | TS core + Rust WASM crypto + Shell scripts (Go CLI pending) |

### Commits (OpenCodeWEBsAG, on `main`)

```
cd7c5c2 Initial commit: OpenCodeWEBsAG Autonomous Polyglot AI Agent
0284693 Create agent-core.yml
6226433 fix: remove unused ledgerPath param from recordBackup
e728358 feat(auth): add GitHub App JWT + installation token auth module
```

---

## Deployed Infrastructure

| Resource | Name | ID / URL |
|----------|------|----------|
| Pages project | `pocwu` | https://pocwu.pages.dev |
| Webhook Worker | `opencodewebsag-worker` | https://opencodewebsag-worker.xup.workers.dev |
| DO Worker | `pocwu-globe-relay` | — |
| KV: Sessions | `SESSIONS_KV` | `f664fcc8a19745f196948cd33ea2fe28` |
| KV: Devices | `DEVICES_KV` | `9631d086c834474f8cd3ee9e65555add` |
| KV: AG Tokens | `AG_TOKENS_KV` | `4746613824af45aba9c043bc20cb09d7` |
| D1 DB | `opencodeabs` | `ac9be9b0-bfd3-498d-b8d8-a38f4d6ecb32` |
| GitHub OAuth App | ID 3749681 | Scope: `read:user` |

### Worker Secrets Set (placeholder — replace with real values)

| Secret | Status |
|--------|--------|
| `WEBHOOK_SECRET` | ⚠️ Placeholder `change-me-in-production` |
| `APP_ID` | ⚠️ Placeholder |
| `INSTALLATION_ID` | ⚠️ Placeholder |
| `PRIVATE_KEY` | ⚠️ Placeholder |

---

## Remaining — Next Session

### Replace Worker Secrets (before real use)

```bash
cd D:\OpenCodeWEBsAG\worker
echo "<real-value>" | npx wrangler secret put WEBHOOK_SECRET
echo "<real-app-id>" | npx wrangler secret put APP_ID
echo "<real-install-id>" | npx wrangler secret put INSTALLATION_ID
# For PRIVATE_KEY, pass the full PEM with newlines:
Get-Content -Raw path\to\private-key.pem | npx wrangler secret put PRIVATE_KEY
```

### If Go is installed — Build CLI

```bash
cd D:\OpenCodeWEBsAG\cmd\pocwu
go build -o ../../bin/pocwu.exe
```

### Verify Pages deployment picks up AG routes

After the next `wrangler pages deploy` of OpenCodeWEBsUI, visit:
- https://pocwu.pages.dev/ag → should show AG Dashboard
- https://pocwu.pages.dev/api/ag/dashboard → should return JSON
- https://pocwu.pages.dev/api/ag/auth/login → should redirect to GitHub

---

## Verification Status (all clean)

| Project | Check | Result |
|---------|-------|--------|
| OpenCodeWEBsUI | `tsc --noEmit` | ✅ Zero errors |
| OpenCodeWEBsUI | `npm run build` | ✅ 64 modules, 2.03s |
| OpenCodeWEBsAG | `npm run build` + `tsc --noEmit` | ✅ Zero errors |
| Worker | `tsc --noEmit` | ✅ Zero errors |
| Worker | `npx wrangler deploy` | ✅ Deployed (12.5s) |
| Worker | GET /health | ✅ `{"status":"ok"}` |
| WASM Crypto | `cargo test` | ✅ 3/3 pass |
| WASM Crypto | `wasm-pack build` | ✅ pkg/ generated |

---

## Env vars needed in OpenCodeWEBsUI (Pages dashboard)

For the AG routes to work properly in production, the following must be set via Cloudflare Pages dashboard → `pocwu` → Settings → Environment variables:

- `GITHUB_CLIENT_ID` — GitHub OAuth App client ID (already set)
- `GITHUB_CLIENT_SECRET` — GitHub OAuth App secret (already set)

No new env vars needed — AG routes use KV bindings and service bindings defined in `wrangler.toml`.
